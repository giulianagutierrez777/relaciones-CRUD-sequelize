<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= movie.title %> </title>
    <link rel='stylesheet' href='/css/style.css' />

</head>
<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
    </figure>
    <h1><%= movie.title %> </h1>    
    <br>
    <p>RATING: <%= movie.rating %> </p>
    <p>AWARDS: <%= movie.awards %> </p>
    <p>LENGTH: <%= movie.length + ' min'%> </p>
    <p>RELEASE DATE: <%= movie.release_date %> </p>

    <% if (movie.genre) { %>
        <h4><a href="/genres/detail/<%= movie.genre.id %>">
            <%= movie.genre.name %>
        </a></h4>
    <% }else{ %>
        <p>SIN ESPECIFICAR</p>
        <% } %>

        <hr>

    <h4>ACTORES</h4>
     <ul>
        <% movie.actors.forEach(actor => { %>
         <li> <a href="/actors/detail/<%= actor.id %>"> <%= actor.first_name %> <b><%= actor.last_name %></b></a></li>
        <% }) %>
     </ul>
    <br>

    <section>
        <!--Aquí debe colocar los botones de Modificar - Borrar y Listado de Películas-->
        <a href="/movies/edit/<%= movie.id %>">EDITAR</a>
        <a href="/movies/delete/<%= movie.id %>">ELIMINAR</a>
    </section>

</body>
</html>